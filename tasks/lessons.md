# Lessons

- **Initial Context**: Always thoroughly read the provided memory constraints. The memory indicated the root API must return JSON, but the codebase had it returning text.
- **Compliance Rules**: Do not blindly accept existing code. The API root returning text was an immediate violation of the declared invariants.